#ask the user for input
word=input("Enter a word")
#use if statement to check if tit is a palindrome
if word==word[::-1]:
    print(word+" is a palindrome!")
else:
    print(word+" is not a palindrome")
